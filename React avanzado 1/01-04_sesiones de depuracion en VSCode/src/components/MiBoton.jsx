import React from 'react'

export default function MiBoton(props) {

    e.target.value
    
    
    const test = (e)=>{
        e.toLowerCase();
    }

    return <button onClick={test} > Test </button>;
}
